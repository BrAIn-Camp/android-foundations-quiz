[
  {
    "id": "ch06-q11",
    "topic": "broadcast intents are one-to-many, not one-to-one",
    "difficulty": "difficult",
    "chapter": "ch-06",
    "snippetLang": "markup",
    "snippet": "Every Intent seen so far in this chapter: one sender,\none resolved receiver (either a named Activity, or one\napp chosen via a chooser).\n\nA different kind of Intent: the OS broadcasts \"the device\nhas finished booting.\" Multiple, entirely unrelated apps\ncan all independently notice and react to this same\nannouncement, with none of them being \"launched\" as a\nvisible screen at all.",
    "question": "What's the structural difference between this broadcast relationship and every Intent relationship covered so far in this chapter?",
    "choices": [
      "There's no real difference — broadcasts still resolve to exactly one target, just like explicit and implicit intents",
      "Every prior example was fundamentally one-to-one (a request resolved to a single receiving component); a broadcast is one-to-many — it's an announcement that any number of independently interested components can react to simultaneously, without any single one of them being \"the\" resolved target",
      "Broadcasts can only be sent by a single specially-designated app on the device",
      "Broadcasts always open a visible screen, just like explicit intents do"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "This introduces a genuinely different shape of relationship than anything else in this chapter. Explicit Intents (Q2) resolve to exactly one named target. Implicit Intents (Q3) resolve to exactly one target too — even with multiple candidates, a chooser (Q4) narrows it down to one. A broadcast breaks that one-to-one pattern entirely: it's a public announcement that many separate components can each notice and act on independently.",
      "lineByLine": [
        "Every earlier example: sender → (resolution process) → exactly one receiving component.",
        "Broadcast example: 'device finished booting' → potentially many apps, each with their own interest in that event, react independently.",
        "None of those reacting components are \"launched\" as a visible foreground screen the way an Activity is — they're simply notified that something happened."
      ],
      "tip": "This is a preview of Chapter 9 (BroadcastReceiver), which covers exactly this one-to-many relationship in depth. For now, the key relational takeaway is that Intents serve two structurally different purposes: directed requests (this chapter's main focus) and public announcements (broadcasts) — don't assume every Intent has exactly one resolved recipient."
    }
  },
  {
    "id": "ch06-q12",
    "topic": "intents can target services too, not just activities",
    "difficulty": "moderate",
    "chapter": "ch-06",
    "snippet": "// Starting an Activity (covered throughout this chapter)\nstartActivity(Intent(this, ForecastDetailActivity::class.java))\n\n// Starting a Service — same Intent mechanism, different\n// kind of component, no visible screen involved\nstartService(Intent(this, WeatherSyncService::class.java))",
    "question": "What does this reveal about the relationship between Intents and the kinds of components they can address?",
    "choices": [
      "Intents are exclusively for Activities; this second example is not actually a real pattern",
      "The same Intent-as-messenger relationship established throughout this chapter isn't limited to Activities — it's a general-purpose way to address ANY declared component, including Services (covered in Chapter 8), which don't show a visible screen at all",
      "startService() secretly creates a hidden Activity behind the scenes",
      "Only implicit Intents can be used to start a Service; explicit Intents only work for Activities"
    ],
    "correctIndex": 1,
    "explanation": {
      "summary": "Everything this chapter has established about Intents — explicit addressing (Q2), the manifest-dependency requirement (Q5), carrying extras (Q6) — applies beyond just Activities. The same messenger relationship is how you address other kinds of declared components entirely.",
      "lineByLine": [
        "`Intent(this, ForecastDetailActivity::class.java)` — targets an Activity, and `startActivity()` is the corresponding call that puts it on the back stack (Chapter 3).",
        "`Intent(this, WeatherSyncService::class.java)` — the exact same explicit-addressing pattern, but naming a Service instead.",
        "`startService()` is the corresponding call for THIS kind of component — no screen appears, no back-stack entry is created, because a Service isn't a visible UI component at all."
      ],
      "tip": "This is a preview of Chapter 8 (Services), which explores what these background, non-visual components actually do. For now, the relational takeaway is: 'Intent' is a general-purpose addressing mechanism across several different kinds of declared components (Activities, Services, and more) — it's not exclusively tied to launching screens."
    }
  }
]
